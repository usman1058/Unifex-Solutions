'use client'

import { motion, useReducedMotion } from 'framer-motion'
import { AlertCircle, ArrowUpRight, CheckCircle2, Clock3, Mail, MapPin, MoveRight, Send, ShieldCheck, Sparkles } from 'lucide-react'
import Link from 'next/link'
import { useState } from 'react'

export default function ContactPage() {
  const reduceMotion = useReducedMotion()
  const [formData, setFormData] = useState({ name: '', email: '', company: '', phone: '', subject: '', message: '' })
  const [status, setStatus] = useState<'idle' | 'submitting' | 'success' | 'error'>('idle')
  const [errorMessage, setErrorMessage] = useState('')

  const handleSubmit = async (event: React.FormEvent) => {
    event.preventDefault()
    setStatus('submitting')
    setErrorMessage('')
    try {
      const response = await fetch('/api/contact', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ ...formData, source: 'contact-page' }) })
      const data = await response.json()
      if (!response.ok || !data.success) throw new Error(data.error?.message || 'Submission failed. Please try again.')
      setStatus('success')
      setFormData({ name: '', email: '', company: '', phone: '', subject: '', message: '' })
    } catch (error) {
      setStatus('error')
      setErrorMessage(error instanceof Error ? error.message : 'Network connection error. Please try again.')
    }
  }

  if (status === 'success') return <main className="flex min-h-screen items-center justify-center overflow-hidden bg-background px-6 pt-24 text-on-surface"><motion.div initial={{ opacity: 0, scale: 0.94 }} animate={{ opacity: 1, scale: 1 }} className="relative w-full max-w-2xl overflow-hidden border border-primary/35 bg-[#151713] p-10 text-center md:p-20"><div className="pointer-events-none absolute left-1/2 top-0 h-64 w-64 -translate-x-1/2 -translate-y-1/2 rounded-full bg-primary/10 blur-3xl" /><CheckCircle2 className="relative mx-auto mb-8 h-16 w-16 text-primary" /><p className="mb-5 text-[10px] font-black uppercase tracking-[0.4em] text-primary">Transmission received</p><h1 className="relative mb-6 font-headline text-5xl font-black uppercase leading-[0.85] tracking-[-0.07em] md:text-7xl">We&apos;ll pick it<br /><span className="text-white/25">up from here.</span></h1><p className="mx-auto mb-10 max-w-md text-sm leading-relaxed text-white/50">Your brief is in the queue. A member of the studio will respond with the next useful question.</p><button onClick={() => setStatus('idle')} className="bg-primary px-7 py-4 text-[10px] font-black uppercase tracking-[0.3em] text-black hover:bg-white">Send another brief</button></motion.div></main>

  return <main className="min-h-screen overflow-hidden bg-background pt-24 text-on-surface">
    <section className="relative border-b border-primary/15 py-24 md:py-36"><div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_78%_28%,rgba(213,255,0,0.14),transparent_25%),linear-gradient(115deg,transparent_0%,rgba(255,255,255,0.025)_50%,transparent_51%)]" /><div className="container relative mx-auto px-6 md:px-12"><motion.div initial={reduceMotion ? undefined : { opacity: 0, y: 30 }} animate={reduceMotion ? undefined : { opacity: 1, y: 0 }} transition={{ duration: 0.9 }}><div className="mb-12 flex items-center justify-between text-[10px] font-black uppercase tracking-[0.45em] text-primary"><span>Unifex / Contact</span><span className="hidden text-on-surface/30 md:block">Brief / 01 — Open channel</span></div><div className="grid max-w-7xl gap-14 lg:grid-cols-[1.1fr_0.9fr] lg:items-end"><h1 className="font-headline text-6xl font-black uppercase leading-[0.8] tracking-[-0.09em] md:text-[9.7rem]">Make<br /><span className="text-on-surface/25">the</span><br />connection.</h1><div><div className="relative mx-auto mb-10 h-48 w-64 [perspective:900px]"><motion.div className="absolute left-1/2 top-1/2 h-28 w-44 -translate-x-1/2 -translate-y-1/2 rotate-[-8deg] border border-primary/70 bg-primary/10" animate={reduceMotion ? undefined : { rotate: [-8, 4, -8], y: [0, -9, 0] }} transition={{ duration: 7, repeat: Infinity, ease: 'easeInOut' }}><Send className="absolute left-1/2 top-1/2 h-10 w-10 -translate-x-1/2 -translate-y-1/2 text-primary" /></motion.div><motion.div className="absolute left-1/2 top-1/2 h-28 w-44 -translate-x-1/2 -translate-y-1/2 rotate-[8deg] border border-white/15" animate={reduceMotion ? undefined : { rotate: [8, -4, 8], y: [0, 10, 0] }} transition={{ duration: 8, repeat: Infinity, ease: 'easeInOut' }} /><div className="absolute left-1/2 top-0 h-full w-px bg-gradient-to-b from-transparent via-primary to-transparent" /></div><p className="max-w-md border-l border-primary/60 pl-6 text-base leading-relaxed text-on-surface/65 md:text-lg">Tell us what is stuck, what is changing, or what needs to exist. We&apos;ll return with a useful next move.</p></div></div></motion.div></div></section>

    <section className="container mx-auto grid gap-14 px-6 py-16 md:px-12 md:py-24 lg:grid-cols-[0.7fr_1.3fr]"><aside className="space-y-10"><div><p className="mb-5 text-[10px] font-black uppercase tracking-[0.4em] text-primary">The response path</p><h2 className="font-headline text-5xl font-black uppercase leading-[0.88] tracking-[-0.07em] md:text-7xl">A clear<br /><span className="text-on-surface/25">next step.</span></h2></div><div className="space-y-4"><div className="border border-outline-variant/15 bg-surface p-6"><Clock3 className="mb-8 h-5 w-5 text-primary" /><h3 className="font-headline text-2xl font-black uppercase">Within 1–2 days</h3><p className="mt-3 text-sm leading-relaxed text-on-surface/50">We review the brief and respond with fit, questions, or a direction.</p></div><div className="border border-outline-variant/15 bg-surface p-6"><ShieldCheck className="mb-8 h-5 w-5 text-primary" /><h3 className="font-headline text-2xl font-black uppercase">No dead ends</h3><p className="mt-3 text-sm leading-relaxed text-on-surface/50">If we are not the right fit, we&apos;ll say so clearly and point you somewhere useful.</p></div></div><div className="flex flex-wrap gap-5 text-[9px] font-black uppercase tracking-[0.25em] text-on-surface/45"><span className="flex items-center gap-2"><Mail className="h-4 w-4 text-primary" /> hello@unifex.com</span><span className="flex items-center gap-2"><MapPin className="h-4 w-4 text-primary" /> Global / remote</span></div></aside>

      <div className="relative overflow-hidden border border-primary/30 bg-[#151713] p-7 md:p-12"><div className="pointer-events-none absolute -right-20 -top-20 h-64 w-64 rounded-full border border-primary/15" /><div className="relative"><div className="mb-12 flex items-end justify-between gap-6"><div><p className="mb-4 text-[10px] font-black uppercase tracking-[0.4em] text-primary">Project brief</p><h2 className="font-headline text-4xl font-black uppercase tracking-[-0.06em] md:text-6xl">Start with<br /><span className="text-white/25">context.</span></h2></div><Sparkles className="h-7 w-7 text-primary" /></div>{status === 'error' && <div className="mb-8 flex items-center gap-3 border border-red-400/40 bg-red-400/10 p-4 text-xs text-red-300"><AlertCircle className="h-4 w-4 shrink-0" />{errorMessage}</div>}<form onSubmit={handleSubmit} className="space-y-8"><div className="grid gap-8 md:grid-cols-2">{[['name', 'Your name', 'Jane Smith'], ['email', 'Email', 'you@company.com'], ['company', 'Company', 'Company name'], ['phone', 'Phone', '+1 000 000 0000']].map(([key, label, placeholder]) => <label key={key} className="block"><span className="mb-3 block text-[9px] font-black uppercase tracking-[0.3em] text-on-surface/55">{label}{(key === 'name' || key === 'email') && ' *'}</span><input type={key === 'email' ? 'email' : 'text'} required={key === 'name' || key === 'email'} value={formData[key as keyof typeof formData]} onChange={(event) => setFormData({ ...formData, [key]: event.target.value })} placeholder={placeholder} className="w-full border border-outline-variant/25 bg-background/60 px-4 py-4 text-xs text-white outline-none transition-colors placeholder:text-on-surface/25 focus:border-primary" /></label>)}</div><label className="block"><span className="mb-3 block text-[9px] font-black uppercase tracking-[0.3em] text-on-surface/55">What are we solving? *</span><input type="text" value={formData.subject} onChange={(event) => setFormData({ ...formData, subject: event.target.value })} placeholder="A product, a system, a security question..." className="w-full border border-outline-variant/25 bg-background/60 px-4 py-4 text-xs text-white outline-none transition-colors placeholder:text-on-surface/25 focus:border-primary" /></label><label className="block"><span className="mb-3 block text-[9px] font-black uppercase tracking-[0.3em] text-on-surface/55">The useful detail *</span><textarea required rows={6} value={formData.message} onChange={(event) => setFormData({ ...formData, message: event.target.value })} placeholder="Tell us what is happening, what success looks like, and where you are now." className="w-full resize-none border border-outline-variant/25 bg-background/60 px-4 py-4 text-sm leading-relaxed text-white outline-none transition-colors placeholder:text-on-surface/25 focus:border-primary" /></label><button type="submit" disabled={status === 'submitting'} className="group flex w-full items-center justify-between border-t border-primary/25 pt-6 text-left"><span className="font-headline text-3xl font-black uppercase tracking-[-0.05em] transition-colors group-hover:text-primary md:text-5xl">{status === 'submitting' ? 'Sending...' : 'Send the brief.'}</span><span className="flex h-14 w-14 items-center justify-center rounded-full bg-primary text-black transition-transform group-hover:rotate-45"><ArrowUpRight className="h-6 w-6" /></span></button></form></div></div>
    </section>

    <section className="border-t border-primary/15 bg-primary py-20"><div className="container mx-auto flex flex-col gap-6 px-6 md:flex-row md:items-center md:justify-between md:px-12"><p className="max-w-xl font-headline text-3xl font-black uppercase leading-none tracking-[-0.04em] text-black md:text-5xl">Prefer a guided start?</p><Link href="/order" className="inline-flex items-center gap-3 self-start bg-black px-6 py-4 text-[10px] font-black uppercase tracking-[0.3em] text-white hover:bg-white hover:text-black">Choose a service <MoveRight className="h-4 w-4" /></Link></div></section>
  </main>
}
